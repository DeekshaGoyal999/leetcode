class Solution:
    def numUniqueEmails(self, emails: List[str]) -> int:
        '''m=[]
        for i in range(len(emails)):
            a=emails[i].split("@")
            p=""
            for j in range(len(a[0])):
                if a[0][j]==".":
                    continue
                if a[0][j]=="+":
                    break
                p+=a[0][j]
            p=p+"@"+a[1]
            m.append(p)
        return len(set(m))'''
      
        
        
        
        
        
        
